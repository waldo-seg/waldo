#!/usr/bin/env bash


# this should be a script that checks for pytorch, and if not installed,
# instructs the user to install it.  (It should mention scripts/dependencies/install_pytorch.sh
# in the error message).
exit 0;
